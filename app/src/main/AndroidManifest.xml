<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.towerofflamingblames">
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_main"
        android:supportsRtl="true"
        android:theme="@style/noActionBar">
        <activity android:name=".GameActivity" />
        <activity android:name=".GameStatistics.StatisticsActivity" />
        <activity android:name=".GameStatistics.StatisticsTypeActivity" />
        <activity android:name=".NicknameActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>